<Page x:Class="MasterFloor.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MasterFloor.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" x:Name="btnAddPartner" Content="Добавить" Click="btnAddPartner_Click" HorizontalAlignment="Center" Margin="10"/>

        <ListView Grid.Row="1" Name="ListViewPartners" MouseDoubleClick="ListViewPartners_MouseDoubleClick" d:ItemsSource="{d:SampleData ItemCount=2}" Margin="5">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding partners_types.title}" FontSize="18"/>
                                    <TextBlock Text="|" FontSize="18"/>
                                    <TextBlock Text="{Binding title}" FontSize="18"/>
                                </StackPanel>
                                <TextBlock Text="{Binding director}"/>
                                <TextBlock Text="{Binding phone}"/>
                                <TextBlock Text="{Binding rating, StringFormat='Рейтинг: \{0}'}"/>
                            </StackPanel>

                            <TextBlock Grid.Column="1" Text="{Binding Discount}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
