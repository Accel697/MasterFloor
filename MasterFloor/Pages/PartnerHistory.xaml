<Page x:Class="MasterFloor.Pages.PartnerHistory"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MasterFloor.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PartnerHistory">

    <Grid>
        <ListView Name="ListViewHistory" d:ItemsSource="{d:SampleData ItemCount=2}" Margin="5">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding products.title}" Margin="10"/>
                            <TextBlock Text="{Binding qunatity, StringFormat='Количество: \{0}'}" HorizontalAlignment="Center" Margin="10"/>
                            <TextBlock Text="{Binding date, StringFormat='dd.MM.yyyy'}" HorizontalAlignment="Right" Margin="10"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
